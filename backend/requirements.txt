# ── Pipeline engine (Phase 1) ────────────────────────────────────────
httpx>=0.27
tenacity>=9.0
pydantic>=2.7
pydantic-settings>=2.3
rich>=13.7
aiosmtplib>=3.0

# ── Web product: API + workers (Phase 2) ─────────────────────────────
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0
alembic>=1.13
celery[redis]>=5.4
redis>=5.0
pyjwt>=2.9
bcrypt>=4.1
email-validator>=2.1

# DB drivers: aiosqlite (local default) + asyncpg (Postgres)
aiosqlite>=0.20
asyncpg>=0.30

# ── Tests ────────────────────────────────────────────────────────────
pytest>=8.0
pytest-asyncio>=0.23
